# 27B推理路由

本模块只负责27B模型的推理调度：

- OpenAI兼容接口的流式转发；
- 请求优先级、排队、取消和超时；
- 27B GPU任务的准入和租约；
- 断线清理和幽灵请求回收；

本模块不保存模型权重，不查询Elasticsearch，也不负责业务任务编排。

源码直接位于当前目录，单元测试和测试Web位于`qwen-llm/tests/`，服务名为
`gta-ai-router.service`。

# 环境声音分析服务

使用固定版本 AudioSet AST 模型把每条音轨按时间窗分类为语音、音乐、车辆、鸟鸣、流水等声音事件。
服务只输出候选标签、置信度和时间范围，不把声音标签混入 ASR 文本。

- 健康检查：`GET /health`
- 推理接口：`POST /v1/audio-events`，请求体为 FLAC
- 默认端口：`18085`
- 容器只在启动时读取本地 Hugging Face 缓存，运行期不联网下载模型。
- 测试节点使用 `node.test.env`，与生产服务端口、模型契约一致但不共享进程。
